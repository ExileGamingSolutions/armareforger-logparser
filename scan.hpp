#include <iostream>
#include <iomanip>
#include <fstream>
#include <string>
#include <sstream>

std::string filePath = "test.txt";
std::ifstream read;
std::fstream write;
std::string x;
std::string a;

std::stringstream ss(x);

